<?xml version="1.0" encoding="utf-8" ?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_order"
        android:title="Ordenamiento..."
        android:icon="@drawable/ic_refresh"
        app:showAsAction="never">

        <item
            android:id="@+id/action_ord_name"
            android:title="Ordenar por Nombre"
            android:icon="@drawable/ic_refresh"
            app:showAsAction="never"/>
        <item
            android:id="@+id/action_ord_status"
            android:title="Ordenar por estatus"
            android:icon="@drawable/ic_refresh"
            app:showAsAction="never"/>
        <item
            android:id="@+id/action_ord_date"
            android:title="Ordenar por fecha"
            android:icon="@drawable/ic_refresh"
            app:showAsAction="never"/>

    </item>
    <item
        android:id="@+id/action_search"
        android:title="#caja"
        android:icon="@drawable/ic_search"
        app:showAsAction="always"
        app:actionViewClass="android.widget.SearchView"
      />
    <item
        android:id="@+id/action_recharge"
        android:title="Regargar lista"
        android:icon="@drawable/ic_refresh"
        app:showAsAction="always"
        />
</menu>